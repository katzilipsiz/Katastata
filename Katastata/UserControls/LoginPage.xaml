<UserControl x:Class="Katastata.UserControls.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Katastata.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BrownBack1" Color="#26221F"/>
        <SolidColorBrush x:Key="RedBack1" Color="#8C2703"/>
    </UserControl.Resources>
    
    <Grid Background="{StaticResource BrownBack1}">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="600"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1"
              Background="{StaticResource RedBack1}">
            
        </Grid>
    </Grid>
</UserControl>
