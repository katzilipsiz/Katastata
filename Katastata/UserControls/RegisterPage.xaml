<UserControl x:Class="Katastata.UserControls.RegisterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Margin="0 0 0 10"
                 Text="{Binding RegisterUsername, Mode=TwoWay}"/>

        <PasswordBox Grid.Row="1" Margin="0 0 0 10"
                     PasswordChanged="RegisterPasswordBox_PasswordChanged"/>

        <Button Grid.Row="2" Content="Зарегистрироваться"
                Command="{Binding RegisterCommand}"
                Padding="10" Background="#007ACC" Foreground="White"/>

        <TextBlock Grid.Row="3" Foreground="Red" Text="{Binding ErrorMessage}"/>
    </Grid>
</UserControl>
